import { render, screen } from '@testing-library/react';
import App from './App';

/*
Need to implement testing - needs more research
examples include -
-assuring that the dom displays correctly
-that navigation logic is correct and is not pointing outside of 
defined pages (so that users are not directed away from the website)

*/

/*test('renders learn react link', () => {
  render(<App />);
  const linkElement = screen.getByText(/learn react/i);
  expect(linkElement).toBeInTheDocument();
});*/
